# 浏览器请求地址；开发时 /api 由 Vite 转发，生产时由 Go 服务直接处理。
VITE_API_BASE_URL=/api
# 仅开发服务器使用，不会暴露给浏览器。
API_PROXY_TARGET=http://localhost:18080
# Orval 直接读取后端 OpenAPI 接口，不保存本地文档。
OPENAPI_INPUT=http://localhost:18080/openapi.json
